# Infrastructure migrations

This folder is reserved for backend database migrations and schema evolution files.

- Keep SQL files organized by version.
- Prefer idempotent migration scripts when appropriate.
- Keep raw telemetry schema and derived data logic separate from app logic.
